#include <stdio.h>

int main()
{
    int marks;
    // printf("enter marks: ");
    // scanf("%d", &marks);
    // marks >=30 && marks <= 100 ? printf("pass \n") : printf("fail \n");
    if(a){
        printf("yes");
    }
    return 0;
}